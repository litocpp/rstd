[package]
name = "rstd-std"
version.workspace = true
license.workspace = true
authors.workspace = true

[lib]
name = "rstd-std"
module = "rstd"
archive = "rstd"

[dependencies]
rstd-core = { workspace = true, pub = true }
rstd-alloc = { workspace = true, pub = true }
