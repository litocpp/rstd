[package]
name = "rstd-toml-conformance-tests"

[[test]]
name = "rstd-toml-conformance-tests"
sources = ["conformance_decoder.cpp"]

[dependencies]
rstd-std = { path = "../../src/std" }
rstd-json = { path = "../../src/json" }
rstd-toml = { path = "../../src/toml" }
rstd-test = { path = "../../src/test" }
